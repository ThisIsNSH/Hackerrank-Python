def print_formatted(number):
    count=0
    a = str(bin(number))
    for i in a:
        count+=1
    count-=2
    for n in range(1,number+1):
        print str(n).rjust(count)+" "+str(oct(n)[1:]).rjust(count)+" "+str(hex(n)[2:]).upper().rjust(count)+" "+str(bin(n)[2:]).rjust(count)
    
